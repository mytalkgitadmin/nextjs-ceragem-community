// 채널 생성 API 계약 타입
import type {
  CreateChannelRequestDTO,
  CreateChannelResponseDTO,
} from "./dto-types";

// 채널 생성 API 요청
export type CreateChannelRequest = CreateChannelRequestDTO;

// 채널 생성 API 응답
export type CreateChannelResponse = CreateChannelResponseDTO;
